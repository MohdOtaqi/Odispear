# ===========================================
# Unity Platform - Environment Variables
# ===========================================
# These are the variables you need to set in Railway

# ===========================================
# DATABASE (Railway provides this automatically)
# ===========================================
# Railway will auto-inject DATABASE_URL when you add PostgreSQL
# DATABASE_URL=postgresql://username:password@host:5432/database

# ===========================================
# JWT Authentication (REQUIRED)
# ===========================================
# Generate: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# ===========================================
# Server Configuration (REQUIRED)
# ===========================================
NODE_ENV=production

# ===========================================
# Daily.co Voice Chat (REQUIRED for voice)
# ===========================================
# Get these from https://dashboard.daily.co/developers
DAILY_API_KEY=your-daily-api-key
DAILY_DOMAIN=your-domain.daily.co

# ===========================================
# Redis (OPTIONAL - Railway can provide this)
# ===========================================
# Add Redis service in Railway and it will auto-inject
# REDIS_URL=redis://...

# ===========================================
# File Upload (OPTIONAL)
# ===========================================
# CLOUDINARY_URL=cloudinary://...
